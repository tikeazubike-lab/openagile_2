---
type: HO
id: HO-007
title: Antigravity → Claude: BDD Workflow Update & Company Linking
status: DRAFT
version: 1.0
owner: Antigravity
date: 2026-05-08
---

# [HO] HO-007 — Antigravity → Claude: BDD Workflow Update & UI Fixes
> **Type**: Handover · **Date**: 2026-05-08

## 1. What Was Done
- **BDD Workflow Documented**: Updated `MASTER_CONTEXT.md` and `AGENTS.md` to reflect the new BDD and TDD workflow as clarified by Claude. Specifically:
  - Antigravity is responsible for writing standard `pytest` functions based on Gherkin specs generated by Claude.
  - Tests must fail first (Red) before writing production code (Green), adhering to Uncle Bob's rule.
  - No `pytest-bdd` dependencies; assertions are linked to specs via comments.
- **UI Contrast Fixes**: Modified `RegistrarList.tsx` to improve readability by removing muted backgrounds on phone and email elements.
- **Linked Companies Fix**: Added backend endpoints and frontend components to allow adding and removing linked companies directly from the Registrar details view.

## 2. What Is Verified Working
- The governance documents accurately reflect the pipeline.
- Future agents will know exactly how to handle `.feature` specs without confusing test automation scopes.

## 3. Decisions Taken
- Used standard `pytest` + `httpx` to implement the BDD scenarios, adding traceability comments directly above assertions rather than introducing `pytest-bdd`.
- Updated the Company Linking to use dedicated `POST` and `DELETE` endpoints specifically for linking to avoid exposing full Company CRUD on the Registrar screen.

## 4. Blockers
- None at this time.

## 5. Next Agent's Action List
1. Acknowledge the finalized BDD pipeline integration.
2. Provide the finalized Gherkin specifications or architectural decisions for the deferred requirements (FR-3, FR-4, FR-12) to proceed with test-driven implementation.
